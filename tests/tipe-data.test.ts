describe('Data Type', () => {
	it('should be must declare', () => {
		let name: string = 'vzents'
		let balance: number = 100000
		let isVip: boolean = true

		console.info(name)
		console.info(balance)
		console.info(isVip)
	})
})
